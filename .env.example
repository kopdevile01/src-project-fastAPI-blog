# --- PostgreSQL ---
DATABASE_URL=postgresql+psycopg://fastapi_user:fastapi_password@localhost:5432/fastapi_blog_db
TEST_DATABASE_URL=postgresql+psycopg://fastapi_user:fastapi_password@localhost:5432/fastapi_blog_db_test

# --- JWT ---
JWT_SECRET=CHANGE_ME
JWT_ALG=HS256
JWT_EXP_MINUTES=60

# --- SMTP (MailHog по-умолчанию) ---
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=noreply@example.com

# --- RabbitMQ ---
RABBITMQ_URL=amqp://guest:guest@localhost:5672/

# --- S3 / MinIO ---
S3_ENDPOINT=http://localhost:9000
S3_ACCESS_KEY=minio
S3_SECRET_KEY=minio123
S3_BUCKET=blog-images
S3_PUBLIC_URL=http://localhost:9000

# =========================
# Если бекенд запускается ВНУТРИ docker-compose
DATABASE_URL=postgresql+psycopg://fastapi_user:fastapi_password@db:5432/fastapi_blog_db
RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672/
SMTP_HOST=mailhog
S3_ENDPOINT=http://minio:9000
(S3_PUBLIC_URL обычно остаётся http://localhost:9000 для доступа из хоста)
